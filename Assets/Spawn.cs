using System.Collections;
using System.Collections.Generic;
using UnityEngine;




public class Spawn : MonoBehaviour
{

    void Start()
    {
        Tileset ts = new Tileset([(0, 0)]);
        
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
